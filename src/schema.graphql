# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Auth {
  """JWT access token"""
  accessToken: String!

  """JWT refresh token"""
  refreshToken: String!
  user: User!
}

type Category {
  """Identifies the date and time when the object was created."""
  createdAt: DateTime!
  description: String
  id: ID!
  name: String!
  posts: [Post!]!

  """Identifies the date and time when the object was last updated."""
  updatedAt: DateTime!
}

input ChangePasswordInput {
  newPassword: String!
  oldPassword: String!
}

input CreatePermissionInput {
  description: String
  name: String!
}

input CreatePostInput {
  categoryName: String!
  content: String
  title: String!
}

input CreateRoleInput {
  description: String
  name: String!
  permissionIds: [String!]
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

input LoginInput {
  email: String!
  password: String!
}

type Mutation {
  changePassword(data: ChangePasswordInput!): User!
  createPermission(data: CreatePermissionInput!): Permission!
  createPost(data: CreatePostInput!): Post!
  createRole(data: CreateRoleInput!): Role!
  deletePermission(id: String!): Permission!
  deletePost(id: String!): Post!
  deleteRole(id: String!): Role!
  login(data: LoginInput!): Auth!
  refreshToken(token: String!): Token!
  signup(data: SignupInput!): Auth!
  updatePermission(data: UpdatePermissionInput!): Permission!
  updatePost(data: UpdatePostInput!): Post!
  updateRole(data: UpdateRoleInput!): Role!
  updateUser(data: UpdateUserInput!): User!
}

"""
Possible directions in which to order a list of items when provided an `orderBy` argument.
"""
enum OrderDirection {
  asc
  desc
}

type PageInfo {
  endCursor: String
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
  startCursor: String
}

type Permission {
  """Identifies the date and time when the object was created."""
  createdAt: DateTime!
  description: String
  id: ID!
  name: String!

  """Identifies the date and time when the object was last updated."""
  updatedAt: DateTime!
}

type Post {
  author: User!
  category: Category!
  content: String

  """Identifies the date and time when the object was created."""
  createdAt: DateTime!
  id: ID!
  title: String!

  """Identifies the date and time when the object was last updated."""
  updatedAt: DateTime!
}

type PostConnection {
  edges: [PostEdge!]
  pageInfo: PageInfo!
  total: Int!
}

type PostEdge {
  cursor: String!
  node: Post!
}

input PostOrder {
  direction: OrderDirection!
  field: PostOrderField!
}

"""Properties by which post connections can be ordered."""
enum PostOrderField {
  content
  createdAt
  id
  published
  title
  updatedAt
}

"""Post status"""
enum PostStatus {
  ARCHIVED
  DELETED
  DRAFT
  PUBLISHED
}

type Query {
  categories: [Category!]!
  category(id: String!): Category!
  me: User!
  permission(id: String!): Permission!
  permissions: [Permission!]!
  post(postId: String!): Post!
  posts(after: String, before: String, first: Int, last: Int, orderBy: PostOrder, query: String, skip: Int): [Post!]!
  publishedPosts(after: String, before: String, first: Int, last: Int, orderBy: PostOrder, query: String, skip: Int): PostConnection!
  queryCategories(query: String!): [Category!]!
  role(id: String!): Role!
  roles: [Role!]!
  userPermissions(id: String!): [Permission!]!
  userPosts(userId: String!): [Post!]!
}

type Role {
  """Identifies the date and time when the object was created."""
  createdAt: DateTime!
  description: String
  id: ID!
  name: String!
  permissions: [Permission!]!

  """Identifies the date and time when the object was last updated."""
  updatedAt: DateTime!
  users: [User!]!
}

input SignupInput {
  email: String!
  password: String!
  username: String
}

type Token {
  """JWT access token"""
  accessToken: String!

  """JWT refresh token"""
  refreshToken: String!
}

input UpdatePermissionInput {
  description: String
  id: String!
  name: String
}

input UpdatePostInput {
  content: String
  id: String!
  status: PostStatus
  title: String
}

input UpdateRoleInput {
  description: String
  id: String!
  name: String
  permissionIds: [String!]
}

input UpdateUserInput {
  username: String
}

type User {
  """Identifies the date and time when the object was created."""
  createdAt: DateTime!
  email: String!
  id: ID!
  posts: [Post!]!
  role: Role!

  """Identifies the date and time when the object was last updated."""
  updatedAt: DateTime!
  username: String
}
